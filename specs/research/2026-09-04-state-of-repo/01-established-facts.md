# A0 — Facts established directly by the orchestrator (2026-09-04)

These were measured/observed first-hand this session (not by a finder agent). Writers may cite them as established.

## Snapshot
- Repo `masra91/Vellum-KB`, local `~/source/KB-App`; review worktree `.clubhouse/agents/KB-Special-Agent` reset to `origin/main` @ `19bb68f` ("chore: fix corrupted CLAUDE.md, untrack .claude/skills, ignore agent runtime dirs (#583)", 2026-07-13).
- 506 commits, first 2026-05-30, last 2026-07-13; single git author (Mason Allen); 14 Clubhouse agent personas in `.clubhouse/agents.json` (roles: group-project-pm, qa ×2, executor-merge ×7, ui-lead ×2, quality-auditor, plus KB-Lead and KB-Special-Agent).
- Commits per ISO week: W22 22 · W23 257 · W24 68 · W26 113 · W27 3 · W28 42 · W29 1. `main` dormant for 7 weeks at review time.
- 714 tracked files, 9.07 MB tracked bytes. Largest tracked: brand/mockups/explore.png 679 KB, the-line.png 633 KB, app/package-lock.json 431 KB, build/icon.icns 220 KB, design-prototypes/vellum-v3.html 177 KB, two showcase e2e snapshots ~168 KB each, app/src/index.css 137 KB.
- No root README.md, CHANGELOG.md, CONTRIBUTING.md, or LICENSE file tracked (package.json declares MIT). READMEs exist only under app/eval/*, brand/, specs/.
- specs/: 63 spec files + INDEX/README/TEMPLATE/SPEC-0000; SPEC-0044 is absent from both the tree and the INDEX (numbering gap); prior deep review at specs/research/2026-07-12-deep-review/.

## GitHub state
- 3 open issues (#531 daemon RFC P1, #56 macOS TCC-protected vaults "MUST", #579 brand comment sweep P2). 0 open PRs. 0 tags. 0 releases. 0 milestones.
- Labels present include `goobers`, `goobers:approved`, `goobers:ready`, `goobers:claimed`, `goobers:nominated`, `goobers:needs-human`, `goobers/status:in-review`, `nightly-red`, `e2e`, `macos`, `deep-review-0712`, `area:*`, `P0/P1/P2`. No `goobers:cloud`, no `release-blocker` label yet.
- The 27 deep-review issues (#505–#531 minus #531) all closed by 2026-07-13. Closed WITH a linked PR: #505(542) #509(543) #510(552) #511(558) #512(566) #516(553) #517(545) #525(536) #527(567) #540(564) #559(563) #574(581) #580(582) #192(571). Closed WITHOUT a linked PR reference: #506 #507 #508 #513 #514 #515 #518 #519 #520 #521 #522 #523 #524 #526 #528 #529 #530 #537 #539 #576 #205.
- Ruleset 17072781 "main: require CI + PR" active (3 required contexts per ci.yml comments). Branch-protection API returns 404 (rulesets, not classic protection).

## CI facts
- ci.yml: last 30 runs all `success`, ~3 min each (07-12/13). Jobs: quick (typecheck·lint·unit+coverage), secrets (gitleaks 8.30.1 via curl|tar, no checksum), package (ubuntu build-check + better-sqlite3 unpacked assert), package-macos (push-to-main / `macos` label / dispatch; 10× billing), e2e (opt-in label/dispatch; macos+windows), snapshots (dispatch-only, xvfb linux, commits baselines back).
- nightly.yml: scheduled 06:17 UTC daily + Sunday eval. EVERY scheduled run since the first (2026-07-13) is `failure` — 55+ consecutive red days. Latest (33845332067, 2026-09-04): knip success (0 min), package-macos success (5 min), e2e macos FAILURE (9 min), e2e windows FAILURE (7 min), eval-scorecard skipped (no EVAL_COPILOT_TOKEN), report-red FAILURE (0 min).
- report-red job error (verbatim): `##[error]An error occurred trying to start process '/usr/bin/bash' with working directory '/home/runner/work/Vellum-KB/Vellum-KB/app'. No such file or directory` — the workflow-level `defaults.run.working-directory: app` applies to a job that never checks out the repo, so the step cannot start. Consequence: zero `nightly-red` issues were ever filed (label exists, no issues). The red lane was invisible for 7 weeks.
- nightly e2e failures are identical on macOS and Windows (deterministic): activity.e2e.ts:52 (`.activity-empty` not found), ask.e2e.ts:51 (`.ask-transcript` not found), jobs.e2e.ts:53 (`.agents-hub-title` not found).
- release.yml has run exactly once: 28308929235 on 2026-06-28, `failure` (push event).
- Node: CI pins 22; local dev machine has Node 24.18.1 / npm 11.16.0 (npm ci prints `allow-scripts` warnings for esbuild/fsevents install scripts).

## Local validation (this machine, worktree @19bb68f, after `npm ci`)
- `npm run typecheck` → exit 0. `npm run lint -- --max-warnings 0` → exit 0.
- `TZ=UTC npm test` → 253 files, 3,097 passed, 1 skipped, 1 todo, 98 s wall (586 s test time).
- `npm run knip -- --reporter compact` → exit 1: ~30 unused exports, 51 unused exported types, 3 duplicate exports (appendix A5).
- `npm audit` → 46 vulnerabilities: 3 critical, 36 high, 4 moderate, 3 low (appendix A4). Direct prod deps affected: js-yaml 4.1.0 (high; prototype pollution via merge keys; fix 4.3.2), dompurify 3.4.5 (moderate; XSS classes; fix 3.4.14), electron 42.2.0 (high; fix 42.11.2). Dev: @vitest/coverage-v8 3.2.4 (critical; fix 3.2.7), the @electron-forge 7.x chain (high; "fix" is a downgrade to 6.4.2 = advisory noise). `npm outdated`: electron 44.2.0, @github/copilot-sdk 1.0.11 (pinned 1.0.0-beta.7), vite 8, vitest 5, typescript 7, chokidar 5, pdfjs-dist 6.
- `npm run package` (macOS arm64) → exit 0 in ~3 min. Bundle 298 MB (Frameworks 266 MB, app.asar 27 MB, app.asar.unpacked 3.9 MB). Natives unpacked: fsevents.node, better_sqlite3.node (+ test_extension.node). Info.plist: CFBundleIdentifier com.vellum.app, version 0.1.0, LSMinimumSystemVersion 12.0, LSUIElement false, CFBundleIconFile electron.icns (content == build/icon.icns, so the brand icon IS applied on macOS). Signature: adhoc, Identifier com.github.Electron (unsigned, unnotarized). `build/` has icon.icns + icon.png but NO icon.ico → Windows packages get the default Electron icon. asar contains 196 node_modules entries (chokidar, marked, dompurify, better-sqlite3, electron-squirrel-startup, …) alongside the .vite bundles. NSDocumentsFolderUsageDescription text reads "…your knowledge-base vault…" (user-visible OS prompt; lexicon law bans generic "vault").
- Local e2e (`ALLOW_LOCAL_E2E=1`, CPU heavily loaded by parallel agents at the time): 5 failed / 2 skipped / 7 passed. Same 3 failures as nightly (activity, ask, jobs) + smoke SETUP-1 "PERF-R5 no white first-frame flash" (window not visible at assertion; page snapshot shows the Setup wizard rendered — possibly load/timing) + walkthrough (all 22 screenshots written, then `afterEach` `app.close()` exceeded the 60 s test timeout — the app did not close promptly after a seeded-vault session). Artifacts: appendix A8_e2e_local (output, error-context DOM snapshots, 22 screenshots).

## Field evidence (running instance)
- userData dirs present: `~/Library/Application Support/Vellum` (live), plus `vellum`, `kb-app`, `KB-App` (earlier product names). Vellum/kb-app.config.json → `{"activeVaultPath": "/Users/masonallen/kb-test-1"}`. conversations/ empty. Crashpad empty. No DiagnosticReports for the app.
- Vellum/logs/app.log: 36,094 lines, 5.2 MB, 2026-06-28T04:33Z → 2026-08-11T06:37Z; ONLY `mem.sample` (36,037) and `mem.leak-suspected` (57) events — no stage/error events. RSS by day: June 28–July 5 min 319–363 / max 595–655 MB; July 13 onward min 84–245 / max 212–649 MB (typical 150–300 MB). Sampling gaps 07-06→07-12 and 08-03→08-06 (app not running).
- kb-app/last-crash.json: 2026-06-13 `render-process-gone` "renderer gone: killed (exit 15)" during stage compose, item "Matching Cat Shirts".
- Vault ~/kb-test-1: git 1,954 commits (all "promote: evergreen → main" at the tip), last 2026-08-07; ≥10 leftover `kb/compose-work-<ulid>` branches plus kb/archive-work, kb/claims-work; sources 336 files, entities 769, claims 3,852, outputs 1. `.kb/cache/spans.jsonl` 6,351 spans (claims 2,468 · compose 1,939 · connect 1,696 · decompose 246 · archive 2); error spans: "decompose: no JSON object in output" ×13, "block timeout reached" ×8, one "Command failed: copilot -p …". `.kb/ask/audit.jsonl` 1 + `.kb/cache/ask/audit.jsonl` 31 recall entries (earliest 2026-06-02 error: "Could not find @github/copilot package…").
- Local tooling: GitHub Copilot CLI 1.0.81 at /opt/homebrew/bin/copilot; gh authenticated as masra91.

## Isolated e2e re-run (smoke + walkthrough only, load avg ~4.6 at start)
- smoke.e2e.ts: 4/4 passed (SETUP-1 boot, PERF-R5 hidden-until-ready, TEST-4 packaged boot survival 9.0 s, PERF-R6 qcap chunk isolation). ⇒ the earlier PERF-R5 failure was ENVIRONMENT (CPU load), not a regression.
- walkthrough.e2e.ts: freshness gate passed; the LIVE WALKTHROUGH wrote all 22 screenshots (1.1 min) and then FAILED AGAIN in `afterEach` — `await app.close()` exceeded the 60 s test timeout. Reproduced twice (loaded and unloaded machine). The app does not exit when Playwright closes it after a seeded-vault session; CI's nightly macOS run does NOT list walkthrough among its failures, so the behavior differs between this machine and the CI runner (to be classified by the visual/runtime lane: tray/keep-alive lifecycle vs. a quiesce/drain hang).
- Quit-path reading (app/src/main.ts:176-210, app/src/main/pipelineLifecycle.ts:596-631): `before-quit` → preventDefault → `stopPipelineForQuit()` (promote flush bounded at 2,000 ms) → `stopTelemetry()` → `app.quit()` again (guarded by `quitInProgress`); `will-quit` → `qcapAgent?.stop()` + best-effort `stopRecallClient()`; `window-all-closed` keeps the app alive on macOS (QCAP-8). So the 60 s `app.close()` hang in the walkthrough is NOT the promote flush; candidates: Playwright's close semantics vs the macOS keep-alive lifecycle, a throwing `will-quit` handler, or something re-entering `before-quit`. Unclassified here — see chapter 08/14.
- CORRECTION (from the visual/runtime QA lane, A9): the walkthrough `afterEach` overrun is a TEST-BUDGET defect, not a quit hang — playwright.config.ts:16 sets one global `timeout: 60_000` that also covers hooks, and the 22-capture body alone ran 1.1 min, so teardown started already over budget. The smoke PERF-R5 failure is ENVIRONMENT (single non-polled `isVisible()` read right after domcontentloaded while `show()` is wired to the later `ready-to-show`). The three nightly failures (activity/ask/jobs) are TEST-DRIFT: the DOM snapshots show each feature present; selectors changed (`.activity-empty` → shared `emptyState()` primitive `.viz-empty__*`; `.ask-transcript` → `#askTranscript`/`.ask-refs`; `.agents-hub-title` → `.ag-top > h1`).
